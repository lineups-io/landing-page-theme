import styled from 'styled-components'

import Description from 'gatsby-theme-atomic-design/src/atoms/LandingPage/Description'

const CustomDescription = styled(Description).attrs({
  type: 'gray200',
})`
`

export default CustomDescription
