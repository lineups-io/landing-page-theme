import styled from 'styled-components'

import ScrollMenu from 'gatsby-theme-atomic-design/src/atoms/ScrollMenu/Container'

const CustomScrollMenu = styled(ScrollMenu).attrs({
  type: 'white',
})`
`

export default CustomScrollMenu
